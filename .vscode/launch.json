{
    "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach (Docker backend)",
      "type": "python",
      "request": "attach",
      "connect": { "host": "localhost", "port": 5000 },
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}/backend",
          "remoteRoot": "/app"
        }
      ]
    }
  ]
}